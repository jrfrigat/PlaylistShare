<FlareNavMenu AriaLabel="Основная навигация">
    <HeaderContent>
        <a href="/" class="deka-nav-brand">
            <span class="deka-nav-brand-mark">
                <DekaMark Size="20" />
            </span>
            <span class="deka-nav-brand-name">PlaylistShare</span>
        </a>
    </HeaderContent>

    <ChildContent>
        <AuthorizeView>
            <Authorized>
                <FlareNavLink Href="/my-playlists">
                    <IconContent><FlareIconView Value="@MaterialDesign3Icons.Regular.QueueMusic" /></IconContent>
                    <ChildContent>Мои плейлисты</ChildContent>
                </FlareNavLink>
                <FlareNavLink Href="/search">
                    <IconContent><FlareIconView Value="@MaterialDesign3Icons.Regular.Search" /></IconContent>
                    <ChildContent>Поиск</ChildContent>
                </FlareNavLink>
            </Authorized>
            <NotAuthorized>
                <FlareNavLink Href="/" Match="NavMatchMode.Exact">
                    <IconContent><FlareIconView Value="@MaterialDesign3Icons.Regular.Home" /></IconContent>
                    <ChildContent>Главная</ChildContent>
                </FlareNavLink>
            </NotAuthorized>
        </AuthorizeView>
    </ChildContent>

    <FooterContent>
        <AuthorizeView>
            <Authorized>
                <a href="/profile" class="deka-nav-account">
                    <FlareAvatar Text="@InitialOf(context.User.Identity?.Name)" Size="AvatarSize.Sm" />
                    <span class="deka-nav-account-name">@(context.User.Identity?.Name ?? "Профиль")</span>
                </a>
            </Authorized>
            <NotAuthorized>
                <FlareNavLink Href="/login">
                    <IconContent><FlareIconView Value="@MaterialDesign3Icons.Regular.Login" /></IconContent>
                    <ChildContent>Вход</ChildContent>
                </FlareNavLink>
            </NotAuthorized>
        </AuthorizeView>
    </FooterContent>
</FlareNavMenu>

@code {
    private static string InitialOf(string? name) =>
        string.IsNullOrWhiteSpace(name) ? "?" : name.Trim()[..1].ToUpperInvariant();
}
